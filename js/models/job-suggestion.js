define(["jquery",
        "backbone"
        ], function($, Backbone){

    var JobSuggestion = Backbone.Model.extend({
       initialize: function  (argument) {
           
       } 
    });
    return JobSuggestion;
});
